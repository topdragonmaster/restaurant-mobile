import { getColor } from '../../../helpers'

export default {
  iconContainer: {
    bg: {
      default: getColor('white'),
    },
    borderColor: {
      default: getColor('santasGray'),
    },

    isChecked: {
      bg: {
        default: getColor('royalBlue'),
      },
      borderColor: {
        default: getColor('royalBlue'),
      },
    },
  },
}
