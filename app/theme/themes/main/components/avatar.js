import { getColor } from '../../../helpers'

export default {
  container: {
    bg: {
      primary: getColor('white'),
      secondary: getColor('gunPowder'),
    },
    borderColor: {
      primary: getColor('gunPowder'),
      secondary: getColor('white'),
    },
  },

  placeholder: {
    color: {
      primary: getColor('gunPowder'),
      secondary: getColor('white'),
    },
  },

  placeholderIcon: {
    tintColor: {
      primary: getColor('gunPowder'),
      secondary: getColor('white'),
    },
  },
}
