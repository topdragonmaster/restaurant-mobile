import { getColor } from '../../../helpers'

export default {
  color: {
    primary: getColor('persimmon100'),
  },

  isPressed: {
    color: {
      primary: getColor('persimmon75'),
    },
  },
}
